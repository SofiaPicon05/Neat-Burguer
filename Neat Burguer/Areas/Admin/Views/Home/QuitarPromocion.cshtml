@model Neat_Burguer.Areas.Admin.Models.ViewModels.QuitarPromocionViewModel
@addTagHelper *, Microsoft.AspNetCore.Mvc.TagHelpers
@{
    Layout = "LayoutAdmin";
}

<form action="~/admin/promocion/quitar" method="post">
    <h1>Eliminar promoción</h1>
    <input asp-for="IdMenu" hidden />
    <span class="pregunta"><b>¿Desea eliminar la promoción de la hamburguesa @(Model.Nombre)?</b></span>
    <span>
        <label for="nombre">Nombre:</label>
        <input asp-for="Nombre" type="text" id="nombre" required>
    </span>
    <span>
        <label for="precio">Precio real:</label>
        <input asp-for="PrecioOriginal" type="text" id="precio" required>
    </span><span>
        <label for="promocion">Precio promocion:</label>
        <input asp-for="PrecioNuevo" type="text" id="promocion" required>
    </span>
    <span class="error">@Html.ValidationSummary(true)</span>

    <div class="botones">
        <input type="submit" value="Si" />
        <a asp-area="admin" asp-controller="home" asp-action="menu">
            <input type="button" value="No" />
        </a>
    </div>
</form>

<script>
    function previewImagen() {
        const inputImagen = document.getElementById('imagen');
        const imagenPreview = document.getElementById('imagen-preview');

        const archivoImagen = inputImagen.files[0];

        if (archivoImagen) {
            const lector = new FileReader();

            lector.onload = function (e) {
                imagenPreview.src = e.target.result;
                imagenPreview.style.display = 'block';
            };

            lector.readAsDataURL(archivoImagen);
        } else {
            imagenPreview.src = '#';
            imagenPreview.style.display = 'none';
        }
    }
</script>
